Quick Settings tile to start/stop sharing without opening the app; persistent notification gains a Stop action and notification permission is now requested on Android 13+. Fixed tap-to-open path validation (traversal via ../ was possible), the Android 15 foreground-service 6-hour force stop, startup blocking the UI thread for up to ~4s, and startup occasionally validating a stale dufs process. Desktop start now verifies the port is listening; corrupt config or keyring failures no longer white-screen at launch. Server status and error strings translated (en/zh-TW). Release artifacts now include checksums.txt.
