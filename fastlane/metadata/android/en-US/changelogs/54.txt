Service verification: startup now asynchronously checks availability (port listening + HTTP response) and rolls back on failure; the Start/Stop button flips instantly without waiting for validation
Fixed a fake-running state after a quick stop-and-restart: startup now verifies the child process is alive and cleans up leftovers, so the UI no longer shows running while nothing is reachable
Desktop stop latency reduced from ~1s to ~100ms